# Computer Science Fundamentals: 100 Difficult Objective Questions

Each question has four options. Choose the best answer.

## Questions

1. Which statement best separates computer science from simple programming?

   A. Computer science studies problem solving, computation, and system design; programming is one tool used to implement solutions.  
   B. Computer science is only about writing code in many languages.  
   C. Programming is theoretical, while computer science is only practical.  
   D. Computer science is limited to hardware repair and networking.

2. A student says, "If I can code, I fully understand computer science." What is the strongest correction?

   A. Coding is irrelevant to computer science.  
   B. Computer science is only mathematics and never uses code.
   C. Coding only matters in web development.  
   D. Coding is important, but computer science also includes algorithms, data structures, theory, systems, and problem analysis.  

3. Why is Ada Lovelace historically important in computing?

   A. She built ENIAC.  
   B. She wrote an algorithm intended for a machine, making her an early computer programmer.  
   C. She invented the transistor.  
   D. She created the World Wide Web.

4. Which invention most directly enabled computers to become smaller, faster, and more reliable than vacuum-tube machines?

   A. Slide rule  
   B. Abacus  
   C. Transistor  
   D. Punch card

5. What made Babbage's Analytical Engine conceptually closer to modern computers than earlier calculators?

   A. It had ideas resembling memory, control flow, and an arithmetic logic unit.  
   B. It was powered by the internet.  
   C. It was the first machine to use Python.  
   D. It stored data in cloud databases.

6. Which description best matches a Turing machine's role in computer science?

   A. A commercial laptop design  
   B. A mathematical model for reasoning about computation  
   C. A debugging tool in Python  
   D. A database indexing strategy

7. Which condition is required for an algorithm to be well-defined?

   A. It must use recursion.  
   B. It must have no inputs.
   C. It must run in O(1).  
   D. Every step must be precise and unambiguous.  

8. Which algorithm characteristic is violated if a procedure can run forever for some valid input?

   A. Definiteness  
   B. Finiteness  
   C. Output  
   D. Effectiveness

9. An algorithm for finding the maximum value initializes `max_value` to the first item, then scans the remaining items once. What is its worst-case time complexity?

   A. O(1)  
   B. O(log n)  
   C. O(n)  
   D. O(n^2)

10. Why does "effectiveness" matter in an algorithm?

    A. Each step must be basic enough to be carried out in finite time.  
    B. The algorithm must be written in Python.  
    C. The algorithm must avoid all loops.  
    D. The algorithm must use the fewest possible variables.

11. Python is described as "dynamically typed." What does this imply?

    A. Variable types are fixed forever once the program starts.  
    B. Python only supports numeric data.
    C. Python programs cannot use functions.  
    D. Type checking is associated with values at runtime rather than requiring explicit type declarations everywhere.  

12. What does Python being "interpreted" usually mean for beginners?

    A. Code is typically executed by an interpreter rather than manually compiled by the learner before every run.  
    B. Python cannot run on real computers.  
    C. Python ignores syntax errors.  
    D. Python converts every variable to a string.

13. Which Python feature is most connected to the phrase "batteries included"?

    A. Significant whitespace  
    B. A broad standard library for common tasks  
    C. Mandatory object-oriented programming  
    D. Absence of third-party packages

14. In Python, what is the most likely purpose of running `python --version`?

    A. To create a new Python project automatically  
    B. To run all tests in a folder
    C. To delete old Python files  
    D. To verify that Python is installed and see the installed version  

15. Why does Python emphasize indentation?

    A. Indentation helps define code blocks and supports readability.  
    B. Indentation is ignored by the interpreter.  
    C. Indentation only affects comments.  
    D. Indentation makes Python run in O(1).

16. Which is the best example of decomposition?

    A. Writing one huge function that handles every task  
    B. Splitting meal preparation into shopping, chopping, cooking, and serving steps  
    C. Removing all function names from a program  
    D. Sorting a list without comparing values

17. A program has separate functions for reading input, validating data, calculating results, and printing output. Which computational thinking idea is most visible?

    A. Decomposition  
    B. Randomization  
    C. Brute force only  
    D. Hardware virtualization

18. Which statement best describes abstraction?

    A. It exposes every internal detail at once.  
    B. It means avoiding models.
    C. It prevents programs from using functions.  
    D. It hides unnecessary implementation details and keeps the useful interface visible.  

19. In a `Car` class with `start()`, `accelerate()`, and `brake()` methods, what abstraction is being provided?

    A. Users can operate the car through simple commands without knowing every internal mechanism.  
    B. Users must manually control every engine component.  
    C. The car cannot have state.  
    D. The class removes all behavior.

20. Pattern recognition helps algorithm design mainly because it lets us:

    A. Ignore repeated structure in data.  
    B. identify regularities that can simplify or generalize a solution.  
    C. avoid testing.  
    D. replace all algorithms with random choices.

21. In the sequence `4, 9, 14, 19, 24`, what pattern is present?

    A. Geometric progression with ratio 5  
    B. Palindrome pattern
    C. Fibonacci pattern  
    D. Arithmetic progression with common difference 5  

22. A function checks whether `"racecar"` reads the same forward and backward. What type of pattern is being recognized?

    A. Frequency pattern  
    B. Structural palindrome pattern  
    C. Queue ordering pattern  
    D. Hash collision pattern

23. If a text-analysis program counts how many times each word appears, which pattern category is it mainly using?

    A. Frequency pattern  
    B. Recursion pattern  
    C. Binary search pattern  
    D. Memory allocation pattern

24. Why is pattern recognition useful for automation?

    A. Programs can use recognized regularities to handle many similar cases consistently.  
    B. It prevents code from processing input.  
    C. It removes the need for algorithms to terminate.  
    D. It only works for images.

25. Big O notation primarily describes:

    A. exact runtime in seconds on a specific laptop.  
    B. how resource usage grows as input size grows.  
    C. the number of programming languages used.  
    D. whether code has comments.

26. Which complexity class is usually most scalable among these for large input?

    A. O(n^2)  
    B. O(n log n)  
    C. O(log n)  
    D. O(2^n)

27. Why are constant coefficients dropped in Big O?

    A. They never affect real runtime.  
    B. Big O focuses on growth rate, especially as input size becomes large.  
    C. Constants are syntax errors.  
    D. Constants only exist in recursive algorithms.

28. What is the simplified Big O of `7n^2 + 100n + 3000`?

    A. O(7n^2)  
    B. O(n^2)  
    C. O(n)  
    D. O(3000)

29. A loop over `n` items is followed by a separate loop over `n` items. What is the simplified time complexity?

    A. O(1)  
    B. O(n)  
    C. O(n^2)  
    D. O(log n)

30. A loop over `n` items contains another loop over `n` items. What rule explains the resulting O(n^2)?

    A. Addition for sequences  
    B. Multiplication for nested operations  
    C. Constant dropping only  
    D. Hashing

31. In linear search, what is the best case?

    A. The target is the first item checked.  
    B. The target is the last item checked.  
    C. The target is missing.  
    D. The list is sorted in descending order.

32. In linear search, why is the worst case O(n)?

    A. The search always halves the input.  
    B. It may need to inspect every item.  
    C. It uses nested loops.  
    D. It stores every item twice.

33. What does space complexity measure?

    A. Growth in memory usage as input size grows  
    B. Growth in screen size needed to display output  
    C. Number of CPU cores used  
    D. Number of lines in the source file

34. Why is dynamic array append considered amortized O(1)?

    A. Every append copies all elements.  
    B. Occasional expensive resizes are spread over many cheap appends.  
    C. Append is always O(n^2).  
    D. Arrays never resize.

35. Which is the best reason Big O is not the only factor in choosing an algorithm?

    A. Constants, input size, readability, and hardware can matter in practice.  
    B. Big O is always false.  
    C. Big O only applies to Python.  
    D. Big O ignores input size entirely.

36. Which array operation is typically O(1)?

    A. Reading by index  
    B. Inserting at the beginning  
    C. Deleting from the middle  
    D. Searching by value in an unsorted array

37. Why is inserting into the middle of an array typically O(n)?

    A. The array must be sorted first.  
    B. Elements after the insertion point may need to shift.  
    C. Arrays cannot store numbers.  
    D. Index lookup is impossible.

38. Which statement about strings is most accurate in the course context?

    A. Strings are sequences of characters and are common in text processing.  
    B. Strings are always hash tables.  
    C. Strings cannot be indexed.  
    D. Strings only store numbers.

39. What does Python slice `"python"[1:4]` return?

    A. `"pyt"`  
    B. `"yth"`  
    C. `"tho"`  
    D. `"hon"`

40. In Python negative indexing, what does `arr[-1]` usually access?

    A. The first element  
    B. The last element  
    C. An invalid memory address  
    D. The middle element

41. Why can removing items from a list while iterating over it cause bugs?

    A. The loop index and list contents can shift, causing skipped elements or unexpected behavior.  
    B. Python forbids all deletion.  
    C. Lists cannot contain duplicate values.  
    D. Iteration always sorts the list.

42. A linked list node usually contains:

    A. a value and one or more references to other nodes.  
    B. only an index number.  
    C. a full copy of the whole list.  
    D. only a hash function.

43. Why is random access slower in a linked list than in an array?

    A. Linked lists are always sorted.  
    B. You must follow links node by node to reach a position.  
    C. Linked lists cannot store references.  
    D. Arrays use recursion for every access.

44. In a singly linked list, inserting at the head is O(1) because:

    A. no traversal is needed; the new node points to the old head.  
    B. the whole list must be copied.  
    C. the list must be sorted.  
    D. every node must update two pointers.

45. What extra ability does a doubly linked list provide compared with a singly linked list?

    A. Each node can refer to both next and previous nodes.  
    B. It removes all pointers.  
    C. It forces FIFO behavior.  
    D. It automatically balances itself.

46. What defines a circular linked list?

    A. The last node points back to the first node.  
    B. The first node stores every value.  
    C. Every node has exactly three children.  
    D. It can only contain integers.

47. Which use case best fits a stack?

    A. Processing customers in arrival order  
    B. Undo/redo history  
    C. Storing sorted key-value pairs  
    D. Finding shortest path in an unweighted graph

48. Which stack operation reads the top item without removing it?

    A. Push  
    B. Pop  
    C. Peek  
    D. Enqueue

49. Which queue operation adds an item to the back?

    A. Dequeue  
    B. Enqueue  
    C. Pop  
    D. Traverse

50. Which scenario most naturally uses a queue?

    A. Browser back button  
    B. Print jobs handled in arrival order  
    C. Recursive function calls  
    D. Inorder traversal of a BST

51. Hash tables are especially useful when you need:

    A. random access by numeric index only.  
    B. guaranteed sorted traversal.  
    C. fast average lookup by key.  
    D. only LIFO behavior.

52. Which task is a natural hash table use case?

    A. Storing a complete binary tree only
    B. Reversing a call stack  
    C. Maintaining strict arrival order  
    D. Counting word frequencies in text  

53. What is a hash function's role?

    A. Reverse a linked list.  
    B. Sort a graph topologically.  
    C. Convert a key into an index or bucket location.  
    D. Halve a search interval.

54. In a tree, which node has no parent?

    A. Leaf  
    B. Root  
    C. Child  
    D. Edge

55. In a tree, what is a leaf?

    A. A connection between two vertices  
    B. A node with no parent  
    C. A node with no children  
    D. A hash-table bucket

56. What is the defining rule of a binary search tree in the course material?

    A. The root must always be zero.
    B. Every node has exactly three children.  
    C. Values must be inserted randomly.  
    D. Smaller values go left, larger values go right.  

57. Which BST traversal returns values in sorted order?

    A. Preorder  
    B. Inorder  
    C. Postorder  
    D. Level skipping

58. Why can a balanced BST search be O(log n)?

    A. It stores all items in one array index.  
    B. It scans every node twice.  
    C. Each comparison can eliminate about half of the remaining tree.  
    D. It uses no comparisons.

59. Which structure best models a file system hierarchy?

    A. Single string
    B. Stack only  
    C. Queue only  
    D. Tree  

60. A graph consists mainly of:

    A. keys and hash functions only.  
    B. rows and formulas only.  
    C. vertices and edges.  
    D. a root and no cycles.

61. Which graph type has edges with direction?

    A. Undirected graph  
    B. Directed graph  
    C. Empty graph  
    D. Complete tree

62. Which graph type is best for modeling roads with distances or costs?

    A. Unindexed string  
    B. Stack graph  
    C. Weighted graph  
    D. Constant graph

63. Which graph representation is usually better for sparse graphs?

    A. Sorted string
    B. Adjacency matrix  
    C. Single integer  
    D. Adjacency list  

64. Which graph representation can be natural for dense graphs?

    A. Function call only  
    B. Stack trace  
    C. Adjacency matrix  
    D. Plain text paragraph

65. What is the time complexity of BFS or DFS over a graph represented with adjacency lists?

    A. O(1)  
    B. O(log V)  
    C. O(V + E)  
    D. O(V^3)

66. Bubble sort works mainly by:

    A. splitting the list into halves and merging.  
    B. choosing a pivot and partitioning.  
    C. repeatedly comparing adjacent elements and swapping when out of order.  
    D. sorting digits from least significant to most significant.

67. What is a key idea of selection sort?

    A. Count word frequencies.
    B. Use a queue to visit nodes level by level.  
    C. Repeatedly halve a sorted list.  
    D. Repeatedly select the smallest remaining element and place it in position.  

68. Insertion sort is often efficient for:

    A. weighted graph shortest paths only.  
    B. very large unsorted lists only.  
    C. small or nearly sorted lists.  
    D. hash table lookup only.

69. Merge sort is closely associated with which strategy?

    A. Manual testing
    B. FIFO scheduling  
    C. Linear probing only  
    D. Divide and conquer  

70. Why is merge sort considered stable when implemented properly?

    A. It only works on strings.  
    B. It never uses extra memory.  
    C. It can preserve the relative order of equal elements.  
    D. It always runs in O(1).

71. Quick sort usually works by:

    A. reading keys from a dictionary.
    B. building a queue of all nodes.  
    C. comparing only adjacent pairs once.  
    D. choosing a pivot, partitioning around it, then recursively sorting partitions.  

72. Why can quick sort degrade to O(n^2)?

    A. It never compares values.  
    B. It always uses a queue.  
    C. Poor pivot choices can create highly unbalanced partitions.  
    D. It only works on sorted arrays.

73. Heap sort is based on:

    A. arithmetic progressions.
    B. palindrome detection.  
    C. exception handling.  
    D. heap data structure properties.  

74. Radix sort is most directly based on:

    A. recursive graph traversal.  
    B. comparing every pair with nested loops.  
    C. sorting values digit by digit.  
    D. catching runtime errors.

75. Linear search is preferable to binary search when:

    A. the list has no elements but must return an index.
    B. the data is already sorted and huge.  
    C. the target is guaranteed to be in the middle.  
    D. the data is unsorted and sorting first is not worthwhile.  

76. Binary search has O(log n) time because:

    A. it uses two nested loops.  
    B. it checks every item.  
    C. each step roughly halves the remaining search space.  
    D. it stores values in a hash table.

77. Which condition must hold before using normal binary search?

    A. The list must have duplicate values.
    B. The list must be a linked list.  
    C. The list must contain only strings.  
    D. The list must be sorted according to the search order.  

78. What does binary search usually return when the target is absent in the course example?

    A. The middle value  
    B. 0  
    C. -1  
    D. The list length squared

79. BFS explores a graph:

    A. by selecting random unvisited nodes only.
    B. by always following one branch as deep as possible first.  
    C. by sorting all edge names alphabetically.  
    D. level by level from the starting node.  

80. Why is a queue central to standard BFS?

    A. It guarantees sorted output.  
    B. It reverses all visits.  
    C. It preserves first-discovered, first-processed order.  
    D. It removes the need for a visited set.

81. In BFS, why should nodes often be marked visited when enqueued?

    A. To make the graph weighted.
    B. To force recursion.  
    C. To convert the graph into a tree automatically.  
    D. To avoid placing the same node in the queue multiple times.  

82. BFS is especially useful for:

    A. catching syntax errors.  
    B. sorting digits in integers.  
    C. shortest paths in unweighted graphs.  
    D. selecting pivots for quick sort.

83. DFS explores a graph:

    A. by deleting every visited node.
    B. strictly level by level.  
    C. only through weighted edges.  
    D. by going deep along a branch before backtracking.  

84. Why does DFS need a visited set on graphs with cycles?

    A. To sort nodes by value.  
    B. To make the graph dense.  
    C. To avoid revisiting nodes forever or doing duplicate work.  
    D. To convert recursion to hashing.

85. Recursive DFS uses which underlying runtime structure?

    A. Disk scheduler
    B. Hash collision table  
    C. Adjacency matrix only  
    D. Call stack  

86. Which task is a natural DFS application?

    A. Array index lookup  
    B. First-come, first-served printing  
    C. Detecting cycles or exploring connected components  
    D. Finding Python version

87. What is recursion?

    A. A syntax error in Python.
    B. A loop that never terminates.  
    C. A data structure with only one node.  
    D. A function solving a problem by calling itself on smaller cases.  

88. Which two parts are essential in correct recursion?

    A. Try and except only  
    B. Queue and hash table  
    C. Base case and recursive case  
    D. Pivot and bucket only

89. Why can recursion use more space than iteration?

    A. Iteration stores every previous loop body.
    B. Recursion never terminates.  
    C. Recursion cannot reuse variables.  
    D. Recursive calls add frames to the call stack.  

90. Which task fits recursion naturally?

    A. Reading an array by index  
    B. Printing one fixed string once  
    C. Traversing nested folders or nested structures  
    D. Checking Python installation

91. Linear algebra appears in computer science because it supports topics such as:

    A. file naming conventions only.
    B. syntax errors only.  
    C. browser back buttons only.  
    D. vectors, matrices, graphics, machine learning, and data transformations.  

92. Probability and statistics are useful in computer science for:

    A. uncertainty, sampling, distributions, data analysis, and machine learning.  
    B. replacing all algorithms with guesses.  
    C. making all programs deterministic.  
    D. avoiding testing.

93. What is debugging?

    A. Only formatting source files.
    B. Writing code without reading errors.  
    C. Deleting tests after code passes.  
    D. Finding, understanding, and fixing problems in a program.  

94. Which question is least aligned with good debugging?

    A. What happened?  
    B. Why did it happen?  
    C. How do we fix it without breaking something else?  
    D. How do we guess randomly until something changes?

95. A missing closing parenthesis in `print("hello"` is what kind of bug?

    A. Syntax error  
    B. Logic error  
    C. Performance optimization  
    D. Successful test

96. Code that runs but returns subtraction inside an `add(a, b)` function is what kind of bug?

    A. Syntax error  
    B. Logic error  
    C. Import error only  
    D. Test fixture

97. Which debugging habit is usually best?

    A. Change one thing at a time and retest.  
    B. Change many unrelated files at once.  
    C. Ignore the error message.  
    D. Avoid reproducing the bug.

98. What does the `except` block do in Python error handling?

    A. Handles specific exceptions raised in the `try` block.  
    B. Always runs before `try`.  
    C. Runs only when there is no exception.  
    D. Deletes the traceback automatically.

99. What does the `else` block do in Python error handling?

    A. Runs if no exception occurred in the `try` block.  
    B. Runs no matter what.  
    C. Catches every exception.  
    D. Defines the risky code.

100. Which testing approach checks small units of code, such as individual functions?

     A. Unit testing  
     B. Integration testing  
     C. Manual testing only  
     D. Runtime crashing

## Answer Key

1. A
2. D
3. B
4. C
5. A
6. B
7. D
8. B
9. C
10. A
11. D
12. A
13. B
14. D
15. A
16. B
17. A
18. D
19. A
20. B
21. D
22. B
23. A
24. A
25. B
26. C
27. B
28. B
29. B
30. B
31. A
32. B
33. A
34. B
35. A
36. A
37. B
38. A
39. B
40. B
41. A
42. A
43. B
44. A
45. A
46. A
47. B
48. C
49. B
50. B
51. C
52. D
53. C
54. B
55. C
56. D
57. B
58. C
59. D
60. C
61. B
62. C
63. D
64. C
65. C
66. C
67. D
68. C
69. D
70. C
71. D
72. C
73. D
74. C
75. D
76. C
77. D
78. C
79. D
80. C
81. D
82. C
83. D
84. C
85. D
86. C
87. D
88. C
89. D
90. C
91. D
92. A
93. D
94. D
95. A
96. B
97. A
98. A
99. A
100. A
