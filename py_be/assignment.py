secret_numbers = [9, 15, 23, 42, 7]
guess = 0


"""
while guess not in secret_numbers:
    guess = int(input("Enter number :"))

    if guess not in secret_numbers:
        print(f"your p{guess} is wrong")
    else:
        print(f"{guess} you are correct")

print(f"{guess}")

boy = "vc"
"""

def greet():
    return "hello"

greet()











"""
while guess not in secret_numbers:
    guess = int(input("Enter your guess:"))
    if guess not in secret_numbers:
        print(f"{guess} is the wrong guess, do better")
    elif guess in secret_numbers:
        print(f"{guess} is the right one ")

"""

"""
while guess not in secret_numbers:
    guess = int(input("Enter your guess: "))
    if guess not in secret_numbers:
        print(f"{guess} is the wrong guess, do better")
    elif guess in secret_numbers:
        print(f"{guess} is the right one!")
        
print(f"The secret numbers were {secret_numbers}")
"""